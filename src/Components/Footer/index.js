import React from 'react';
import { Navbar } from 'react-bootstrap';

function Footer() {
    return (

        <section>
            <Navbar bg='dark' variant='dark' expand='lg'  >This is the footer</Navbar>
        </section>
    );
};

export default Footer;