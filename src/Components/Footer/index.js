import React from 'react';
import { Navbar } from 'react-bootstrap';

function Footer() {
    return (
        <section>
            <Navbar variant='dark' expand='lg'  >This is the footer</Navbar>
        </section>
    );
};

export default Footer;