import React from 'react';
import { Navbar } from 'react-bootstrap';

function Nav() {
    return (
        <header>
            <Navbar bg='dark' variant='dark' expand='lg'>This is the Navbar</Navbar>
        </header>
    )
};

export default Nav;